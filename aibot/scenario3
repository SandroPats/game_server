while ($i < 10) {
	$a[$i] := $i;
	$i := $i + 1;
}
$i := 0;
while ($i < 10) {
	print ("a[",$i,"] = ", $a[$i]);
	$i := $i + 1;
}
print ("my id is ", ?my_id);
print ("production price: ", ?production_price);
